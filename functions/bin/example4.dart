void main(List<String> args) {
  // No Return Values
  //  you would never invoke the function and assign the result to variable

  // final void value = greet(); it just assign nothing
}

// some functions do not need to return value so we use void type
//

void greet() {
  print("greet");
}
